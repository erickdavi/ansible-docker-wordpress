# k8s-lab

Este diretório foi criado como um novo repositório base para experimentos com Kubernetes.

## Próximos passos sugeridos

1. Inicializar o Git localmente:
   ```bash
   git init
   ```
2. Criar estrutura inicial (ex.: `manifests/`, `helm/`, `docs/`).
3. Fazer o primeiro commit.
